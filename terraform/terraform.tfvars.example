# Copy to terraform.tfvars and fill in, or pass via -var-file in CI.
aws_region      = "us-east-1"
project_name    = "node-ecs-demo"
github_repo     = "Musty2025x/node-ecs-fargate-cicd"
container_image = "" # first apply can be blank; CI updates it on every deploy